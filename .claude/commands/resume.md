Read docs/PLAN.md. Run `git log --oneline -10` and `git status`.

Summarize:
1. What's been completed so far (per the Status checklist and Log)
2. What phase of the plan we're currently in
3. Any uncommitted or unfinished work from last session
4. Your proposed next step

If the most recent Log entry looks mid-task rather than complete, flag that explicitly rather than assuming it's done.

Wait for my confirmation before writing any code.