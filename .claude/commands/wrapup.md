Update docs/PLAN.md. with:
- Check off any phases completed this session in the Status checklist 
- Add a Log entry under the current phase: what was done, key decisions and why, and what's next 
- If work is mid-phase and unfinished, note that clearly so /resume doesn't assume it's done 
- At the end of each phase, before starting the next, briefly check whether upcoming phases in PLAN.md still make sense given what we've learned. Flag if something should be reordered, merged, split, or dropped.
  
Then check git status for uncommitted changes and flag anything I should commit before I go.